[project]
name = "spike2-llm-agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.79.0",
    "pydantic-monty>=0.0.4",
    "python-dotenv>=1.2.1",
]
