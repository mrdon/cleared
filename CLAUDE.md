# Cleared

Agentic small business accounting. Go runtime + Python agents (Monty sandbox) + CSV data in git.

## Commit Messages

```
prefix: Brief description

- Bullet point 1
- Bullet point 2
```

- Subject: `prefix: Description` (max 50 chars, imperative mood, no period)
- Second line: Always blank
- Body: Short bullets only, wrap at 72 chars
- No co-author lines, no emojis
- Prefixes: `init`, `import`, `categorize`, `confirm`, `correct`, `void`, `reconcile`, `close`, `config`, `bootstrap`, `learn`, `agent`, `test`, `optimize`
- Don't commit unless asked
